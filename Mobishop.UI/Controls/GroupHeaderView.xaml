<?xml version="1.0" encoding="UTF-8"?>
<ContentView
	xmlns="http://xamarin.com/schemas/2014/forms"
	xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
	x:Class="Mobishop.UI.Controls.GroupHeaderView">
	<ContentView.Content>
		<StackLayout
			BackgroundColor="{DynamicResource headerBackgroundColor}"
			Spacing="0">
			<BoxView
				Color="{DynamicResource separatorColor}"
				HeightRequest="1">
			</BoxView>
			<Label
				x:Name="TitleLabel"
				VerticalOptions="CenterAndExpand"
				TranslationX="15"
				TextColor="{DynamicResource textColor}"
				FontFamily="{DynamicResource fontFamilyMedium}"
				FontSize="Small">
			</Label>
			<BoxView
				Color="{DynamicResource separatorColor}"
				HeightRequest="1">
			</BoxView>
		</StackLayout>
	</ContentView.Content>
</ContentView>